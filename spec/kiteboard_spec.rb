require 'spec_helper'

describe Kiteboard do
  it 'has a version number' do
    expect(Kiteboard::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
